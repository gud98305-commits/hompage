# ── 엔진 환경변수 설정 예시 (.env.example) ──
# 이 파일을 .env로 복사한 뒤, 실제 값으로 채워 사용하세요.

# 데이터베이스 URL (기본: SQLite)
DATABASE_URL=sqlite+aiosqlite:///./game.db

# 디버그 모드 (true/false)
DEBUG=false

# CORS 허용 오리진 (쉼표 구분)
CORS_ORIGINS=*

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
